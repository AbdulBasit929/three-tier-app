apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80        # Service port inside cluster
      targetPort: 80  # Frontend container port
      nodePort: 30080 # NodePort for public access

